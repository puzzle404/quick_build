<div class="rounded-lg bg-white p-5 shadow-sm ring-1 ring-slate-200">
  <p class="text-sm text-slate-500"><%= label %></p>
  <p class="mt-2 text-3xl font-semibold text-slate-800"><%= value %></p>
  <span class="<%= delta_classes %>"><%= delta_text.presence || '&nbsp;'.html_safe %></span>
</div>

