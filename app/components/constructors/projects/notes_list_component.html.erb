<div>
  <div style="font-size:11px;font-family:var(--font-mono);color:var(--color-ink-3);text-transform:uppercase;letter-spacing:0.7px;margin-bottom:8px;">
    Notas · <%= notes.size %>
  </div>

  <% if notes.empty? %>
    <div style="padding:14px;text-align:center;color:var(--color-ink-4);font-size:12px;border:1px dashed var(--color-line);border-radius:6px;">
      Aún no hay notas.
    </div>
  <% else %>
    <div style="display:flex;flex-direction:column;gap:8px;">
      <% notes.each do |note| %>
        <div style="border:1px solid var(--color-line);border-radius:6px;padding:10px 12px;background:var(--color-bg-raised);">
          <div style="display:flex;align-items:flex-start;justify-content:space-between;gap:12px;">
            <div style="flex:1;min-width:0;">
              <% if note.title.present? %>
                <div style="font-size:12.5px;font-weight:600;color:var(--color-ink);margin-bottom:3px;"><%= note.title %></div>
              <% end %>
              <div style="font-size:12px;color:var(--color-ink-2);">
                <%= simple_format(note.body) %>
              </div>
              <div style="margin-top:5px;font-size:10px;color:var(--color-ink-4);font-family:var(--font-mono);">
                <%= note.author.email %> · <%= time_ago_in_words(note.created_at) %> atrás
              </div>
            </div>
            <div style="flex-shrink:0;">
              <%= render Qb::BtnComponent.new('Eliminar', variant: :danger, size: :xs,
                    href: delete_path(note),
                    data: { turbo_method: :delete, turbo_confirm: '¿Eliminar esta nota?' }) %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
