# frozen_string_literal: true

# Modal command palette opened with ⌘K (or by clicking the trigger in the
# sidebar footer). Resultados se cargan via fetch a Constructors::SearchController
# — el componente solo provee el shell. Ver qb/cmd_palette_controller.js.
class Qb::Layout::CmdPaletteComponent < ViewComponent::Base
  def initialize(*); end
end
