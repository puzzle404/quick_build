<div class="relative overflow-hidden rounded-xl border border-slate-200 bg-white p-6 shadow-sm transition-all hover:shadow-md hover:border-indigo-200 group">
  <dt>
    <div class="absolute rounded-md bg-indigo-50 p-3">
      <% if icon.present? %>
        <%= icon %>
      <% else %>
        <svg class="h-6 w-6 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3 13.125C3 12.504 3.504 12 4.125 12h2.25c.621 0 1.125.504 1.125 1.125v6.75C7.5 20.496 6.996 21 6.375 21h-2.25A1.125 1.125 0 013 19.875v-6.75zM9.75 8.625c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125v11.25c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 01-1.125-1.125V8.625zM16.5 4.125c0-.621.504-1.125 1.125-1.125h2.25C20.496 3 21 3.504 21 4.125v15.75c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 01-1.125-1.125V4.125z" />
        </svg>
      <% end %>
    </div>
    <p class="ml-16 truncate text-sm font-medium text-slate-500"><%= title %></p>
  </dt>
  <dd class="ml-16 flex items-baseline pb-1 sm:pb-2">
    <p class="text-2xl font-semibold text-slate-900"><%= value.presence || "—" %></p>
    <% if suffix.present? %>
      <span class="ml-1 text-sm font-medium text-slate-500"><%= suffix %></span>
    <% end %>
    
    <% if trend.present? %>
      <p class="ml-2 flex items-baseline text-sm font-semibold <%= trend_color_class %>">
        <%= trend_icon %>
        <span class="sr-only"><%= trend[:direction] == :up ? 'Increased' : 'Decreased' %> by</span>
        <%= trend[:value] %>
      </p>
    <% end %>
  </dd>
  <% if description.present? %>
    <p class="ml-16 text-sm text-slate-500"><%= description %></p>
  <% end %>
</div>
