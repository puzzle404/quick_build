<div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm transition-shadow hover:shadow-md">
  <p class="text-sm font-medium text-slate-500"><%= title %></p>
  <div class="mt-4 flex items-baseline gap-2">
    <span class="text-3xl font-bold tracking-tight text-slate-900"><%= value.presence || "—" %></span>
    <% if suffix.present? %>
      <span class="text-sm font-medium text-slate-500"><%= suffix %></span>
    <% end %>
  </div>
  <% if description.present? %>
    <p class="mt-2 text-sm text-slate-500"><%= description %></p>
  <% end %>
</div>
