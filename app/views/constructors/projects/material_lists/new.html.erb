<%= render Constructors::Projects::SectionsNavComponent.new(project: @project, current_section: :materials) %>

<div class="mt-6 max-w-4xl space-y-6">
  <%= render Ui::SectionHeaderComponent.new(title: 'Nueva lista de materiales', subtitle: 'Cargá manualmente tus ítems o adjuntá archivos que quieras mantener junto a la obra.') %>
  <%= render "form" %>
</div>
