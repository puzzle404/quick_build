<%= render Constructors::Projects::SectionsNavComponent.new(project: @project, current_section: :planning) %>

<div class="mt-6 space-y-6">
  <header class="space-y-2">
    <h1 style="font-size:22px;font-weight:600;color:var(--color-ink);">Editar etapa</h1>
    <p style="font-size:13px;color:var(--color-ink-3);">Actualizá la información principal y las fechas para mantener a tu equipo alineado.</p>
  </header>

  <section class="qb-card">
    <%= render Constructors::Projects::Stages::StageFormComponent.new(project: @project, stage: @stage) %>
  </section>
</div>
