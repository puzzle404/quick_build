<%= render Constructors::Projects::SectionsNavComponent.new(project: @project, current_section: :planning) %>

<div class="mt-6 space-y-6">
  <header class="space-y-2">
    <h1 class="text-2xl font-semibold text-slate-900"><%= @stage.parent.present? ? 'Nueva subetapa' : 'Nueva etapa principal' %></h1>
    <p class="text-sm text-slate-500">
      <%= @stage.parent.present? ? "Asocia esta subetapa a #{@stage.parent.name} para detallar entregables." : 'Define una nueva etapa clave dentro de la planificación de la obra.' %>
    </p>
  </header>

  <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render Constructors::Projects::Stages::StageFormComponent.new(project: @project, stage: @stage) %>
  </section>
</div>
